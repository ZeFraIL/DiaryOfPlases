# תיעוד פרויקט "DiaryOfPlases"

## 1. סקירה כללית

**DiaryOfPlaces** היא אפליקציית אנדרואיד המשמשת כיומן אישי לשמירת מידע על מקומות שונים. האפליקציה מאפשרת למשתמשים לבצע אימות, להוסיף מקומות חדשים עם תיאור מפורט, תמונה, קואורדינטות GPS וקישור אינטרנט, וכן לנהל רשימת אנשי קשר אישיים.

---

## 2. רכיבים מרכזיים

### 2.1. פעילויות (מסכים)

#### `Start.java`
- **קובץ עיצוב:** `activity_start.xml`
- **מטרה:** מסך פתיחה המוצג למשך 5 שניות עם הפעלת האפליקציה.
- **פונקציונליות:** עובר אוטומטית למסך `StartChoise` לאחר סיום הטיימר. לחיצה על המסך קוטעת את הטיימר ומבצעת מעבר מיידי.

#### `StartChoise.java`
- **קובץ עיצוב:** `activity_start_choise.xml`
- **מטרה:** מספק למשתמש בחירה ראשונית: להתחבר למערכת או לעבור לחלק המידע בלבד.
- **פונקציונליות:**
    - כפתור "התחל" (`startUser`): מעבר למסך `LogOrReg`.
    - כפתור "מידע בלבד" (`onlyInfo`): מעבר למסך `OnlyInfo`.
    - תפריט אפשרויות: גישה לחלקים "קרדיטים", "מדריך", "אנשי קשר", "חירום" והפעלה/כיבוי של הסיוע הקולי.

#### `LogOrReg.java`
- **קובץ עיצוב:** `activity_log_or_reg.xml`
- **מטרה:** מסך כניסה או הרשמה למשתמש.
- **פונקציונליות:**
    - **מצב "הרשמה":** אם הקובץ `mypass.txt` לא קיים, מנחה את המשתמש ליצור ולשמור סיסמה.
    - **מצב "כניסה":** אם הקובץ `mypass.txt` קיים, מאמת את הסיסמה שהוזנה.
    - **אחסון:** הסיסמה נשמרת כטקסט רגיל באחסון הפנימי. *הערה: זהו מנגנון לא מאובטח.*

#### `MainChoise.java`
- **קובץ עיצוב:** `activity_main_choise.xml`
- **מטרה:** תפריט ראשי למשתמש מאומת.
- **פונקציונליות:**
    - "הוסף מקום": מעבר למסך `AddPlace`.
    - "הצג מקומות": מעבר למסך `ViewPlaces`.
    - "מידע": מעבר למסך `OnlyInfo`.
    - לפני כל מעבר, מוצגת תיבת דו-שיח לאישור הפעולה.

#### `AddPlace.java`
- **קובץ עיצוב:** `activity_add_place.xml`
- **מטרה:** טופס להוספת רשומה חדשה על מקום.
- **פונקציונליות:**
    - מאפשר הזנת שם והערה.
    - **תמונה:** מפעיל את אפליקציית המצלמה לצילום תמונה (`ACTION_IMAGE_CAPTURE`).
    - **GPS:** מפעיל את `MyLocation` לקבלת קואורדינטות.
    - **קישור:** מפעיל את `Search` לחיפוש וצירוף כתובת URL.
    - **שמירה:** שומר את כל הנתונים, כולל התמונה בפורמט `BLOB`, במסד הנתונים SQLite.

#### `ViewPlaces.java`
- **קובץ עיצוב:** `activity_view_places.xml`
- **מטרה:** הצגת רשימה של כל המקומות השמורים.
- **פונקציונליות:**
    - טוען את שמות המקומות ממסד הנתונים ומציג אותם ב-`ListView`.
    - לחיצה על פריט ברשימה מציגה תיבת דו-שיח עם מידע מלא: שם, הערה ותמונה.

#### `InnerContacts.java`
- **קובץ עיצוב:** `activity_inner_contacts.xml`
- **מטרה:** מרכז ניהול אנשי הקשר.
- **פונקציונליות:**
    - מציג אנשי קשר "פנימיים" (ממסד הנתונים של האפליקציה) או אנשי קשר מספר הטלפונים של המכשיר.
    - עבור אנשי קשר פנימיים, מאפשר ביצוע שיחות, שליחת SMS, שליחת אימייל, עריכה ומחיקה.

#### `AddInnerContact.java`
- **קובץ עיצוב:** `activity_add_inner_contact.xml`
- **מטרה:** טופס ליצירה ועריכה של אנשי קשר "פנימיים".
- **פונקציונליות:** פועל במצב הוספה (טופס ריק) או במצב עדכון (טופס שמולא בנתוני איש הקשר).

#### `Search.java`
- **קובץ עיצוב:** `activity_search.xml`
- **מטרה:** דפדפן אינטרנט מובנה לחיפוש ובחירת כתובת URL.
- **פונקציונליות:** משתמש ב-`WebView` כדי לטעון את חיפוש גוגל עם שאילתה שהועברה מ-`AddPlace`. מחזיר את כתובת ה-URL שנבחרה למסך הקודם.

#### `MyLocation.java`
- **קובץ עיצוב:** `activity_my_location.xml`
- **מטרה:** קבלת קואורדינטות ה-GPS הנוכחיות.
- **פונקציונליות:** משתמש ב-`LocationManager` כדי לקבל את המיקום הידוע האחרון ומחזיר אותו מיד למסך שקרא לו (`AddPlace`).

---

### 2.2. מחלקות עזר ושירותים

#### `HelperDB.java`
- **מטרה:** מחלקת עזר לניהול מסד הנתונים SQLite.
- **פונקציונליות:** יורשת מ-`SQLiteOpenHelper`. אחראית על יצירת מסד הנתונים `info.db` ושתי טבלאות: `Places` ו-`myContacts`.

#### `Place.java`
- **מטרה:** מחלקת מודל (POJO) לייצוג נתונים של מקום בודד.
- **מבנה:** מכילה שדות `placeName`, `placeComment`, `placeImage` (byte[]), `placeLink`, `placeGPS`.

#### `myContact.java`
- **מטרה:** מחלקת מודל (POJO) לייצוג נתונים של איש קשר בודד.
- **מבנה:** מכילה שדות `contactID`, `contactName`, `contactComment`, `contactPhone`, `contactEmail`.

#### `SpeakService.java`
- **מטרה:** שירות להקראת טקסט (סיוע קולי).
- **פונקציונליות:** משתמש ב-`TextToSpeech` כדי להקריא טקסט שהתקבל דרך `Intent`. מופעל מהתפריט ברוב הפעילויות.

---

## 3. ממשק משתמש (UI)

### 3.1. קובצי עיצוב (Layouts)

- `activity_start.xml`: עיצוב מסך הפתיחה.
- `activity_start_choise.xml`: עיצוב מסך הבחירה הראשוני.
- `activity_log_or_reg.xml`: עיצוב מסך הכניסה/הרשמה.
- `activity_main_choise.xml`: עיצוב התפריט הראשי.
- `activity_add_place.xml`: טופס הוספת מקום עם שדות `EditText`, `ImageView` ו-`Button`.
- `activity_view_places.xml`: מכיל `ListView` להצגת רשימת המקומות.
- `activity_inner_contacts.xml`: מכיל `ListView` לאנשי קשר וכפתורים להחלפת מצבים.
- `activity_add_inner_contact.xml`: טופס הוספה/עריכה של איש קשר.
- `mycontact_dialog.xml`: עיצוב מותאם אישית לתיבת הדו-שיח המציגה פרטי איש קשר וכפתורי פעולה.

### 3.2. קבצי התאמה אישית של העיצוב

קבצים אלו נמצאים בתיקייה `res/values/`:

- **`colors.xml`**: מגדיר את הצבעים העיקריים המשמשים באפליקציה (צבעי רקע, טקסט, צבעי הדגשה).
- **`strings.xml`**: מכיל את כל משאבי המחרוזות. שימוש בקובץ זה מאפשר לוקליזציה קלה של האפליקציה לשפות אחרות.
- **`themes.xml` (או `styles.xml`)**: מגדיר את ערכת הנושא הכללית של האפליקציה, כולל תכונות בסיסיות כמו פלטת הצבעים (primary, secondary) וסגנונות ברירת מחדל לכפתורים, שדות טקסט ורכיבים אחרים.
